# 🧪 เครื่องมือวัด · เทสต์ · ด่านตรวจ

_อ่านเมื่อ: เขียนสคริปต์วัด/สแกน · เขียนเทสต์ · เขียนด่านใน migration · ทำ QA หน้าจริง_

> แยกออกมาจาก `CHARTER.md` เมื่อ 2026-07-27 — **เนื้อหาเหมือนเดิมทุกตัวอักษร**
> เหตุผล: CHARTER โตจาก 18k เป็น 116k ตัวอักษรใน 8 วัน ⇒ ทุก session ต้องอ่าน ~17,500 tokens
> ก่อนเริ่มงานแม้แต่บรรทัดเดียว ซึ่งเป็นกับดัก context rot ที่กติกาเหล่านี้ตั้งใจจะกันเอง

**บทเรียนในไฟล์นี้: 12 ข้อ**

---

> 📌 **ที่มาของบทเรียนในไฟล์นี้**
> ทุกข้อมาจาก **ความผิดพลาดที่เกิดขึ้นจริงบนโปรเจกต์ production** (CRM/ERP ของสถาบันสอน AI ในไทย)
> ตัวอย่างจึงอ้างชื่อไฟล์/ตารางของโปรเจกต์นั้น — **เก็บไว้โดยตั้งใจ** เพราะบทเรียนที่ไม่มีเคสจริงกำกับ จะถูกอ่านผ่านแล้วลืม
> อ่านที่ *กลไก* ไม่ใช่ที่ชื่อไฟล์

---


**❄️ อาการที่พึ่ง exit-animation (กล่องปิดไม่สนิท · หน้าค้างคลิกไม่ได้) — ห้ามวัดผ่าน Browser pane ที่ถูกซ่อน** (sales 2026-07-27 · เสนอถ้อยคำเอง · **ราคาของบทเรียนนี้คือ work order ทั้งใบ**)

เบราว์เซอร์**หยุดเดิน CSS animation ของหน้าที่ไม่ได้แสดงผล** ⇒ exit-animation ของ Radix ไม่จบ ⇒ `Presence` ไม่ unmount ⇒ cleanup ของ `DismissableLayer` ไม่ทำงาน ⇒ **`body` ค้าง `pointer-events:none` + กล่องค้างใน DOM**
> **นั่นคืออาการเดียวกับบั๊กจริงเป๊ะ — แต่เกิดจากเครื่องมือ ไม่ใช่โค้ด**

🔴 **เกิดจริง 2026-07-27:** coordinator รับรายงาน *"ปิดฟอร์มขายแล้วทั้งหน้าอัมพาต · ทำซ้ำได้ 100%"* แล้วแตก chip + รายงาน Trin ว่าเป็น **P1 บน prod**
· งวดที่ไปตรวจวัดจริง **22 การปิด ค้าง 0 ครั้ง** (169–250 ms ทุกเคส · 3 ขนาดจอ · ทุกเส้นทางที่ work order สั่ง) ⇒ **บั๊กไม่มีอยู่จริง**

✅ **ด่านที่ต้องผ่านก่อนรายงานอาการตระกูลนี้ — 3 ข้อ:**
1. **วัดเคสควบคุม "ปิดกล่องชั้นเดียว" ในสภาพเดียวกันก่อนเสมอ** — กล่องชั้นเดียวเกิดบั๊กซ้อนชั้นไม่ได้โดยนิยาม · **ถ้าเคสควบคุมก็ค้าง = เครื่องมือพัง ไม่ใช่โค้ดพัง**
2. **เช็ค `document.visibilityState === 'visible'` ก่อนเชื่อผล** — ไม่ visible = **ผลใช้ไม่ได้ทั้งชุด ไม่ใช่แค่คลาดเคลื่อน** · ให้ใช้ `playwright-cli` แทน Browser pane
3. 🪞 **ตัวนับต้องพิสูจน์สภาพ *เริ่มต้น* ด้วย ไม่ใช่แค่สภาพ *ปลายทาง*** (sales 2026-07-27 · เจอกับตัวเองในงวดถัดมา)
   > วัดปุ่มลูกศรได้ `guardAppeared: true` เลยเกือบเขียนว่า "ปิดรูครบทั้ง 2 ทาง" — **ที่จริงกล่องนั้นค้างเปิดมาจากขั้นตอนถ่ายภาพก่อนหน้า** · จับได้เพราะเติมตัวนับ `guardOpenBefore` แล้ววัดใหม่จากสภาพสะอาด → `false`
   >
   > 🔴 **ผลบวกลวงชนิดนี้อันตรายกว่าชนิดในหัวข้อข้างบน:** ข้างบนทำให้ไป**หา**บั๊กที่ไม่มี · ชนิดนี้ทำให้**หยุดหา**รูที่ยังเปิดอยู่จริง
   > ⇒ **ทุกตัวนับที่วัด "มีอะไรโผล่ขึ้นมาไหม" ต้องบันทึกด้วยว่า "ก่อนหน้านั้นมันโผล่อยู่แล้วหรือเปล่า"**

⚠️ **และหลักฐานที่ *ดูเหมือน* ปิดประเด็นได้ อาจตกไปทั้งข้อถ้ามันวัดด้วยวิธีเดียวกัน**
work order ใบนั้นยืนยันว่า "เป็นบั๊กเก่า" ด้วยการสลับไฟล์กลับเป็น `origin/main` แล้วยัง freeze — **แต่วัดด้วยวิธีที่พังเหมือนกัน** ⇒ มันพิสูจน์แค่ว่า**เครื่องมือให้ผลเดิมไม่ว่าโค้ดเป็นอะไร** ซึ่งคนละเรื่องกับ "บั๊กมีมาก่อน"
> 📌 **การทดลองที่ตัวแปรควบคุมไม่ถูกแยก ไม่ได้ให้คำตอบที่อ่อน — มันไม่ได้ให้คำตอบเลย**

🔧 **เครื่องมือพร้อมใช้: [`scripts/team/radix-close-probe.js`](scripts/team/radix-close-probe.js)** — ตอบ 3 สถานะ **`CLEAN` / `FREEZE` / `TOOL_BROKEN`** · มีเคสควบคุมและเช็ค `visibilityState` ในตัว · **พิสูจน์แล้วว่าเปลี่ยนสถานะได้จริงทั้งสองทาง**

**🧪 เขียนสแกน/probe เอง = ต้องสอบเทียบกับเคสที่รู้คำตอบก่อนใช้เสมอ** (students 2026-07-25): shell loop ที่ regex ผิดนิดเดียว **flag 56 ไฟล์ว่าไม่มีใคร import ทั้งที่ของจริงมีตัวเดียว** — ถ้าเชื่อผลดิบแล้วรายงาน = สั่งคนไปลบของที่ใช้อยู่ · **วิธีกัน: รันสแกนกับ 1 เคสที่รู้คำตอบอยู่แล้ว** (เช่นไฟล์ที่แน่ใจว่ามีคน import — ถ้าสแกนบอกว่าไม่มี แปลว่า**สแกนพัง ไม่ใช่โค้ดพัง**) · หลักเดียวกับ "0 offenders อาจแปลว่าหน้าพัง" — **เครื่องมือวัดต้องถูกวัดก่อน**

**📋 สเปคที่กระจายอยู่หลายหัวข้อ = สเปคที่จะหลุดตอน implement** (coordinator 2026-07-26 — ผมทำหลุดเอง) · guard "เคสหัวหน้าทีม" ถูกเขียนไว้ **3 ที่** ในเอกสารเดียวกัน (หัวข้อ 1.3 · 6.1 · 6.1ค ข้อ (ง)) · ผมอ่านหัวข้อ 6.1ค แล้วหยิบมาแค่ guard "ใครอ้างแถวนี้อยู่" **ตกข้อหัวหน้าทีมไปทั้งข้อ** แล้ว migration ก็ผ่าน typecheck/build/tests ครบ เพราะไม่มีเทสต์ไหนรู้จักข้อที่หายไป
> **ก่อนเขียน migration/โค้ดตามสเปค ให้รวบ requirement ทั้งเอกสารเป็น checklist ก้อนเดียวก่อน** แล้วติ๊กทีละข้อ — อย่าอ่านหัวข้อเดียวแล้วลงมือ · **ฝั่งคนเขียนสเปคก็ต้องรวบให้ ไม่ใช่กระจาย**
> **ที่จับได้เพราะมีคนตรวจของจริงหลังลง prod** — data ตรวจฐาน · arch ตรวจ migration · **ทั้งคู่ชี้ตรงกันโดยไม่ได้นัดกัน** และเพราะยังไม่มี UI เรียก ความเสียหายจึงเป็นศูนย์ · **"ลง prod แล้ว" ไม่ใช่ "ตรวจเสร็จแล้ว"**

**🎯 ด่านตรวจใน migration ต้องวัดสิ่งที่ *ขั้นตอนนี้* ทำ ไม่ใช่ความสมบูรณ์แบบของทั้งระบบ** (coordinator 2026-07-26 — ผมพลาดข้อนี้เอง **หลังจากที่ data ทักผมเรื่องเดียวกันเมื่อไม่กี่ชั่วโมงก่อน**)
ผมเขียนด่านให้สแกน `sales` ทั้งตารางว่า "ไม่มี `course_id` กำพร้า" · **migration ล้มทันที** เพราะไปเจอ `SAL20260817` (2026-04-15) ที่มี `course_id: ""` — **สตริงว่าง ไม่ใช่ตัวชี้กำพร้าจริง และไม่เกี่ยวกับงานที่กำลังทำเลย**
> ⇒ **การซ่อมที่ถูกต้องถูกยกเลิกทั้งชุด เพราะของค้างเก่าที่ไม่เกี่ยวกัน** · ถ้าไม่ได้อ่าน error ให้ละเอียด จะสรุปว่า "ข้อมูลมีปัญหา" แล้วไปไล่ผิดที่
> **+ `''` (สตริงว่าง) ไม่ใช่ `null`** — เงื่อนไข `is not null` บน JSONB จับสตริงว่างติดมาด้วยเสมอ · ใช้ `nullif(x,'')` ทุกครั้งที่เช็ค "มีค่าไหม" บนค่าที่มาจาก JSON

**👁️ watcher ที่ไม่เคยเห็นว่าตัวเองเปลี่ยนสถานะได้ = ไม่ใช่ watcher** (data 2026-07-26) · เขียนคิวรีเฝ้า "เคสแรกที่มีคนใช้ลิงก์รับที่นั่ง" แล้ว**ทดสอบใน sandbox ว่าไฟเปลี่ยนจาก ⬜ เป็น 🔴 ได้จริง** ก่อนส่งมอบ
> **"watcher ที่ไม่เคยเห็นว่าเปลี่ยนสถานะได้ คือกับดักเดียวกับ `/claim` ที่ไม่เคยถูกใช้จริง"** — ของที่ไม่เคยทำงานจริงสักครั้ง ไม่มีใครรู้ว่ามันพังจนกว่าจะถึงวันที่ต้องพึ่งมัน · หลักเดียวกับ **🧪 สอบเทียบสแกน** แต่คนละด้าน: สอบเทียบกันผลบวกลวง · ทดสอบว่าไฟเปลี่ยนได้กัน**ผลลบลวงถาวร**

**🧪 ตัวนับใน harness สำคัญกว่าผลของ harness** (sales 2026-07-27) · ดัก `sb.fetch` เพื่อปลอมข้อมูลทดสอบ **แต่ `.from()` ไม่เข้าทางนั้น** (`PostgrestClient` ถือสำเนา `fetch` ตั้งแต่ตอนสร้าง client ต่างจาก `.functions` ที่เป็น getter) ⇒ ของปลอมไม่เคยถูกใช้
> **จับได้เพราะใส่ตัวนับ `__hits` ไว้ แล้วมันรายงานตรงๆ ว่าเป็น 0** · **ถ้าไม่มีตัวนับ จะเห็นเลข layout ไม่เปลี่ยน แล้วสรุปว่า "ทนแล้ว" ทั้งที่ยังไม่ได้ทดสอบอะไรเลย**
> ⇒ **ผลลบลวงที่หน้าตาเหมือนผลบวก** — ทุก harness ที่ปลอม/ดักอะไรก็ตาม **ต้องมีตัวนับพิสูจน์ว่ามันถูกเรียกจริง**

**⏱️ เทสต์ race condition ที่ใช้ helper ระดับผู้ใช้ มักวัดโลกที่ race ไม่มีวันเกิด** (students 2026-07-27) · เทสต์ "กดรัว 2 ครั้ง" ด้วย `userEvent.click` ×2 **เขียวทั้งที่ถอด guard ออก** · เปลี่ยนเป็น `fireEvent.click` ×2 — **ยังเขียวอีก** เพราะทั้งคู่ flush React ระหว่างคลิก ⇒ `disabled` ทันเสมอ
> **ที่แดงได้จริงมีแบบเดียว: `dispatchEvent` สอง event ใน `act` ก้อนเดียว**
> ⇒ **ต้องลองถอด guard ออกดูก่อนเสมอ** ไม่งั้นได้เทสต์ที่ดูครบแต่ไม่เคยตรวจอะไรเลย — หน้าตาใหม่ของกฎ **"watcher ต้องพิสูจน์ว่ามันแดงได้จริง"**

**🔌 ก่อน QA ต้องพิสูจน์ว่ากำลังดูโค้ดของตัวเอง** (students 2026-07-27) · `preview_start` ได้ port อื่นเพราะ `:3000` ถูก server ตัวอื่นยึด ⇒ **เปิด `:3000` ตามความเคยชิน = QA โค้ดของคนอื่นแล้วรายงานว่าผ่าน**
> **วิธีเช็ค 1 คำสั่ง: `curl <port>/src/... | grep -c <symbol ที่งวดนี้เพิ่งเพิ่ม>`** — ถูกกว่ามารู้ทีหลังเสมอ
>
> 🪤 **แต่กับไฟล์ `.tsx` ห้ามค้นด้วยไวยากรณ์ JSX — Vite แปลงทิ้งไปแล้ว** (sales 2026-07-27 · เกือบสรุปผิด)
> `grep -c 'onBack={requestClose}'` → **`0`** ทั้งที่โค้ดขึ้นแล้ว เพราะเซิร์ฟเวอร์เสิร์ฟ **`onBack: requestClose`**
> ✅ **ค้นด้วย *identifier* ที่รอดการแปลง** — ชื่อ prop เปล่าๆ · ชื่อฟังก์ชัน · **สตริงไทยในหน้าจอ**
> 🚫 **ห้ามใช้** `={` · `<Tag` · `className=` · อะไรก็ตามที่มีเฉพาะในไฟล์ต้นทาง
>
> 🔴 **ทิศทางอคติของกับดักนี้คือ *ผลลบลวง* เสมอ** — บอกว่าโค้ดไม่ขึ้นทั้งที่ขึ้นแล้ว ⇒ **ทำให้ไปไล่หาปัญหาที่ไม่มี**
> (คนละทิศกับกับดัก Browser pane ในข้อ ❄️ ซึ่งให้ *ผลบวกลวง*) · **รู้ทิศของอคติแล้วเดาได้ว่าจะพลาดแบบไหน**
>
> ⚖️ **และวิธีที่จับมันได้คือสิ่งที่ควรทำทุกครั้ง: ผล QA ขัดกับตัวพิสูจน์**
> ปุ่มทำงานถูกแล้วแต่ตัวพิสูจน์บอกว่าโค้ดไม่ขึ้น ⇒ **ขัดกันเมื่อไหร่ต้องหาว่าใครผิดก่อนเดินต่อ ห้ามเลือกอันที่ถูกใจ**

**✋ "กดปุ่มของแถวแรกในหน้า" = "แตะข้อมูลลูกค้าจริง" เสมอ — QA ที่ต้องกรอกอะไรลงฟอร์ม ต้องเปิดจากระเบียนที่เราสร้างเอง** (sales 2026-07-27 · รายงานตัวเอง)
> สคริปต์ QA กด `querySelector('button[aria-label="แก้ไขรายการขาย"]')` ⇒ ได้ปุ่มของ**แถวแรกในหน้า** ซึ่งวันนั้นคือ **บิลลูกค้าจริง `SAL20261707`** · พิมพ์ข้อความทดสอบลงช่องหมายเหตุแล้วกด *"ปิดโดยไม่บันทึก"*
> 🟢 ตรวจย้อนแล้วไม่มีอะไรถูกเขียน (หมายเหตุยาว 12 ตัวอักษรเท่าเดิม · `updated_at` เท่าเวลาสร้าง · ไม่มีร่องรอยใน `deletion_history`)
> 🔴 **แต่รอดเพราะบังเอิญ ไม่ใช่เพราะออกแบบไว้** — ถ้าสคริปต์เผลอกด *"บันทึก"* แทน คือแก้บิลลูกค้าจริง
> ✅ **อย่างน้อยที่สุด: ยืนยันเลขระเบียนบนหัว dialog ก่อนพิมพ์อะไรลงไป** · ที่ดีกว่า: สร้างระเบียนของตัวเองแล้วเปิดจากอันนั้น
> 📌 ต่อยอดจากข้อ 🎯 (*วัดผิดตัวอย่าง*) — **ข้อนี้คือ *เขียนใส่ตัวอย่างที่ไม่ใช่ของเรา* ซึ่งแพงกว่าหลายเท่า เพราะแก้คืนไม่ได้ด้วยการวัดใหม่**

**🧭 หัวข้อรวม: เราเชื่อสิ่งที่ *อ่านได้* แทนสิ่งที่ *รันอยู่* — และมันถูกเกือบตลอด จนวันที่มันไม่ถูก** (2026-07-27 · เจอ 3 หน้าตาในวันเดียว)

| สิ่งที่เราอ่าน | สิ่งที่เป็นจริง | ใครเจอ · หลอกไปทางไหน |
|---|---|---|
| **แผน/work order ที่เขียนไว้** | โค้ดที่มีอยู่จริง | students — *"§④ข สั่งใส่ `fillViewport`"* ⇒ คิดว่าใส่แล้วทุกหน้า **ของจริงใส่ 1 จาก 4** |
| **ค่า default ในโค้ด** | ค่าที่ระบบใช้จริง | sales — แก้ `FACTORY_DEFAULTS` แล้วคิดว่าจบ **แต่ 3 แถวใน DB override เสมอ** |
| **ข้อความในไฟล์ต้นทาง** | โค้ดที่ทำงานจริง | sales — `import` · ไวยากรณ์ JSX · **คอมเมนต์ที่อธิบายสิ่งที่ถูกถอดออกไป** |

🔴 **ทั้งสามผ่านการ "ตรวจ" มาแล้วทั้งนั้น** — มี grep · มีเทสต์ · มี request ที่ทำตามครบ · **ที่ขาดคือการไปดูของที่ *รันอยู่จริง***
✅ **ท่าที่ปิดทั้งสามข้อพร้อมกัน: ถอด fix ออกแล้วดูว่ามันพังจริงไหม** — ถ้าถอดแล้วยังเขียว/ยังทำงานได้ **แปลว่าสิ่งที่คุณตรวจ ไม่ใช่สิ่งที่ทำให้มันทำงาน**

**📄 เทสต์ที่ *อ่านซอร์สโค้ด* ต้องตัด `import` (และคอมเมนต์) ทิ้งก่อนตรวจ** (sales 2026-07-27 · เจอกับเทสต์ตัวเอง)

เทสต์รุ่นแรกเช็คว่าชื่อฟังก์ชันโผล่ในไฟล์ (`expect(src).toContain('mergeInstallmentSection')`)
> **ถอดบรรทัดที่เรียกใช้จริงออก → เทสต์ยังเขียว เพราะบรรทัด `import` ก็มีชื่อนั้น**
> ⇒ ได้เทสต์ที่ดูเหมือนล็อกการต่อสาย **แต่ที่จริงล็อกแค่ว่า "ยัง import อยู่"**

✅ **ท่าที่ถูก:** ตัด `import` (และคอมเมนต์) ทิ้งก่อน → นับ **การเรียกใช้** `ชื่อฟังก์ชัน(` **ไม่ใช่** ชื่อฟังก์ชันเปล่าๆ
✅ **และใส่ตัวนับสอบเทียบว่าการตัดไม่ได้กินโค้ดทั้งไฟล์** (ถ้าตัดเกินจนเหลือสตริงว่าง เทสต์จะเขียวด้วยเหตุผลผิดอีกแบบ)
🔑 **จับได้เพราะลองถอด fix ออกดูว่าแดงจริงไหม ไม่ใช่เพราะอ่านเทสต์แล้วดูดี** — ตระกูลเดียวกับ 🪤 ข้อ 🔌 (ค้นด้วยไวยากรณ์ JSX ไม่เจอเพราะ Vite แปลงทิ้ง): **ทั้งคู่คือการค้นข้อความที่ไม่ใช่โค้ดที่ทำงานจริง**

**⚖️ "เจอเยอะผิดปกติ" = เครื่องมือพัง ไม่ใช่โค้ดพัง** (courses 2026-07-25 — เจอกับดักเดียวกับ students คนละวิธี) · ถ้าสแกนบอกว่า 20 ไฟล์ในเขตเป็น dead code **ให้สงสัยสคริปต์ก่อนเสมอ** · courses โดน false positive 2 รอบ: รอบแรก flag ทั้งเขต (regex จับ import path พลาด) รอบสอง flag `InstructorMultiSelect` ว่ากำพร้า — **ที่จริงมีคน import แต่ใช้ double quote ส่วน regex รับแค่ single quote** · ถ้าส่งมาตรงๆ ผมคงลบไปแล้วและฟอร์มสร้าง/แก้รุ่นพังทันที
> **ก่อนเสนอลบไฟล์ไหน ต้อง grep ยืนยันซ้ำแบบไม่ผูก quote style** — `from ['"]…` ทั้งสองแบบ + `import(…)` แบบ dynamic + ค้นชื่อ symbol เปล่าๆ อีกรอบ · **สัญญาณพวกนี้จบที่ "ขอ coordinator ลบไฟล์" ซึ่งผิดแล้วแพง**
